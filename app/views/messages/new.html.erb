<h1>New message to <%= @receiver.full_name %></h1>
<p><%= emojify @message.body %></p>
<%= form_for @message do |f| %>
  <%= hidden_field_tag 'receiver_id', @receiver.id %>

  <%= f.label :body, 'Send a message' %>
  <%= f.text_area :body %>

  <%= f.submit %>
<% end %>
